name: Deploy Flask App (No Tests)

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # Descarga el código

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'  # Ajusta la versión

      - name: Install dependencies
        run: pip install -r requirements.txt  # Instala Flask u otras dependencias

      # --- Elimina o comenta este bloque ---
      # - name: Run tests
      #   run: pytest
      # --------------------------

      - name: Deploy to Vercel (o tu plataforma)
        run: |
          echo "¡Desplegando sin tests!"
          # Comandos específicos para tu despliegue (ej: vercel --prod)
